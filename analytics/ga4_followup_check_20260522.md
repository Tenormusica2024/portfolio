# GA4 follow-up check memo: portfolio self-access opt-out

Created: 2026-05-20 JST
Recommended check window: 2026-05-22 to 2026-05-23 JST

## Purpose

Confirm from GA4 data that owner / AI-agent verification traffic stopped inflating B2B and C2C portfolio counts after the opt-out and `?ga_status=1` diagnostic work.

## What changed

- C2C top-level portfolio pages now support `?ga_off=1` and `?ga_status=1` opt-out flow.
- B2B Ezlize pages now support `?ga_status=1` diagnostic flow in addition to the existing opt-out guard.
- `?ga_status=1` is also an opt-out trigger, so diagnostic visits should not count as normal GA4 traffic.

## Check targets

Use the latest GA4 export / dashboard after enough GA4 processing delay has passed.

1. C2C portfolio
   - Focus paths: `/portfolio/projects.html`, `/portfolio/presentations.html`, other `/portfolio/` pages.
   - Expected: no new large direct / desktop Chrome spike caused by owner/agent verification.

2. B2B Ezlize
   - Focus paths: `/`, `/index.html`, and B2B service pages.
   - Expected: no new large direct / desktop Chrome spike caused by owner/agent verification.

3. Data filter testing
   - Check whether `Developer Traffic` rows appear.
   - Note: Developer Traffic rows are helpful but not the only source of truth; previous portfolio self-access was not fully represented there.

## Pass criteria

- No new obvious self-access spike on 2026-05-20 or later that resembles the 2026-04-26 / 2026-04-28 / 2026-04-29 B2B pattern or the 2026-05-12 / 2026-05-13 C2C pattern.
- If `?ga_status=1` was used for verification, it does not create a normal traffic spike.
- The adjusted report generated by `scripts/generate_portfolio_adjusted_report.py` remains the operational historical correction, not a forensic deletion.

## Commands / artifacts

Generate the current adjusted estimate:

```powershell
cd C:\Users\Tenormusica\portfolio
python -X utf8 .\scripts\generate_portfolio_adjusted_report.py
```

Current generated artifacts:

- `analytics/reports/portfolio_adjusted_access_20260520.md`
- `analytics/reports/portfolio_adjusted_access_20260520.json`

## Guardrails

- Do not treat same-day GA4 values as final; wait for processing delay.
- Do not expose analytics URL, credentials, or private management links in public repo or public pages.
- Treat historical adjusted values as estimates because GA4 aggregate data does not expose IP or exact owner identity.
- In adjusted reports, `excluded users` is a date-level active-user observation sum for the excluded days, not a forensic period-unique user deletion. Treat `adjusted users` as a rough estimate; PV and sessions are the more reliable adjusted metrics.
